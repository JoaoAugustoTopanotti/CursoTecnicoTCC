# Ignorar a pasta node_modules
node_modules/

# Ignorar arquivos de log
*.log

# Ignorar variáveis de ambiente (segredos) — mantenha apenas .env.example versionado
.env*
!.env.example

.vercel

# Pasta com segredos do Firebase Admin (ex.: config/firebaseSecret.json)
config

# Chave do Firebase Admin SDK — NUNCA versionar
src/serviceAccount.json
serviceAccount.json

.next/
